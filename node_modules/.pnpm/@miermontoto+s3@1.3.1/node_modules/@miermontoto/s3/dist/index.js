"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.S3Wrapper = void 0;
const client_s3_1 = require("@aws-sdk/client-s3");
const helpers_1 = require("./helpers");
const DEFAULT_REGION = 'us-east-1';
const DEFAULT_MAX_KEYS = 1000;
class S3Wrapper {
    constructor(config) {
        const clientConfig = {
            bucket: config.bucket,
            region: config.region ?? process.env.AWS_REGION ?? DEFAULT_REGION,
        };
        if (config.credentials?.accessKeyId && config.credentials?.secretAccessKey) {
            clientConfig.credentials = {
                accessKeyId: config.credentials.accessKeyId,
                secretAccessKey: config.credentials.secretAccessKey,
            };
        }
        this.bucket = config.bucket;
        this.defaults = clientConfig;
        this.client = new client_s3_1.S3Client(clientConfig);
    }
    newClient(options = this.defaults) {
        this.bucket = options.bucket;
        const mergedOptions = { ...this.defaults, ...options };
        this.client = new client_s3_1.S3Client(mergedOptions);
    }
    getBucket() {
        return this.bucket;
    }
    setBucket(bucket) {
        this.newClient({ bucket });
    }
    getClient() {
        return this.client;
    }
    async upload(key, body, options) {
        const params = {
            Bucket: this.bucket,
            Key: key,
            Body: body,
            ContentType: options?.contentType,
            Metadata: options?.metadata,
        };
        const response = await this.client.send(new client_s3_1.PutObjectCommand(params));
        return (0, helpers_1.wasSuccessful)(response);
    }
    async download(key, responseType = 'text') {
        const params = {
            Bucket: this.bucket,
            Key: key,
        };
        const response = await this.client.send(new client_s3_1.GetObjectCommand(params));
        if (!response.Body) {
            throw new Error('Empty response body');
        }
        if (!(0, helpers_1.wasSuccessful)(response)) {
            throw new Error('Failed to download file');
        }
        switch (responseType) {
            case 'text':
                return await response.Body.transformToString();
            case 'stream':
                return response.Body.transformToWebStream();
            case 'buffer':
            default:
                return await response.Body.transformToByteArray();
        }
    }
    async delete(key) {
        const params = {
            Bucket: this.bucket,
            Key: key,
        };
        const response = await this.client.send(new client_s3_1.DeleteObjectCommand(params));
        return (0, helpers_1.wasSuccessful)(response);
    }
    async exists(key) {
        const params = {
            Bucket: this.bucket,
            Key: key,
        };
        try {
            const response = await this.client.send(new client_s3_1.HeadObjectCommand(params));
            return (0, helpers_1.wasSuccessful)(response);
        }
        catch (error) {
            if (error.name === 'NotFound' || error.$metadata?.httpStatusCode === 404) {
                return false;
            }
            throw error;
        }
    }
    async metadata(key) {
        const params = {
            Bucket: this.bucket,
            Key: key,
        };
        return await this.client.send(new client_s3_1.HeadObjectCommand(params));
    }
    async list(options) {
        const params = {
            Bucket: this.bucket,
            Prefix: options?.prefix,
            MaxKeys: options?.maxKeys ?? DEFAULT_MAX_KEYS,
            ContinuationToken: options?.continuationToken,
        };
        return await this.client.send(new client_s3_1.ListObjectsV2Command(params));
    }
    async copy(sourceKey, destKey, destBucket = this.bucket) {
        const params = {
            Bucket: destBucket,
            CopySource: `${this.bucket}/${sourceKey}`,
            Key: destKey,
        };
        const response = await this.client.send(new client_s3_1.CopyObjectCommand(params));
        return (0, helpers_1.wasSuccessful)(response);
    }
    async move(sourceKey, destKey, destBucket = this.bucket) {
        const copyResult = await this.copy(sourceKey, destKey, destBucket);
        if (copyResult) {
            return await this.delete(sourceKey);
        }
        return copyResult;
    }
    async getBucketRegion(bucketName = this.bucket) {
        try {
            const response = await this.client.send(new client_s3_1.GetBucketLocationCommand({ Bucket: bucketName }));
            return response.LocationConstraint;
        }
        catch (error) {
            throw error;
        }
    }
}
exports.S3Wrapper = S3Wrapper;
__exportStar(require("./helpers"), exports);
__exportStar(require("./interfaces"), exports);
//# sourceMappingURL=index.js.map