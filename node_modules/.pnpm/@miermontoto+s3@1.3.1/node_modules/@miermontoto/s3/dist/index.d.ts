import { S3Client, type HeadObjectCommandOutput, type ListObjectsV2CommandOutput } from '@aws-sdk/client-s3';
import { S3WrapperConfig } from './interfaces';
export declare class S3Wrapper {
    private client;
    private bucket;
    private readonly defaults;
    constructor(config: S3WrapperConfig);
    newClient(options?: S3WrapperConfig): void;
    getBucket(): string;
    setBucket(bucket: string): void;
    getClient(): S3Client;
    upload(key: string, body: Buffer | Uint8Array | string | ReadableStream, options?: {
        contentType?: string;
        metadata?: Record<string, string>;
    }): Promise<boolean>;
    download(key: string, responseType?: 'text' | 'buffer' | 'stream'): Promise<string | Uint8Array | ReadableStream>;
    delete(key: string): Promise<boolean>;
    exists(key: string): Promise<boolean>;
    metadata(key: string): Promise<HeadObjectCommandOutput>;
    list(options?: {
        prefix?: string;
        maxKeys?: number;
        continuationToken?: string;
    }): Promise<ListObjectsV2CommandOutput>;
    copy(sourceKey: string, destKey: string, destBucket?: string): Promise<boolean>;
    move(sourceKey: string, destKey: string, destBucket?: string): Promise<boolean>;
    getBucketRegion(bucketName?: string): Promise<string | undefined>;
}
export * from './helpers';
export * from './interfaces';
